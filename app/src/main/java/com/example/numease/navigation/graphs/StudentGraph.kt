package com.example.numease.navigation.graphs

import androidx.navigation.NavController
import androidx.navigation.NavGraphBuilder
import androidx.navigation.compose.composable
import androidx.navigation.compose.navigation
import androidx.navigation.toRoute
import com.example.numease.navigation.*

fun NavGraphBuilder.studentGraph(navController: NavController) {
    // startDestination cần truyền một data class với giá trị mặc định tạm thời

}