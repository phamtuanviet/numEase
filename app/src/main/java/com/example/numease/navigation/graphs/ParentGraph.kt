package com.example.numease.navigation.graphs

import androidx.navigation.NavController
import androidx.navigation.NavGraphBuilder

fun NavGraphBuilder.parentGraph(navController: NavController) {
    // Tạm để rỗng
}